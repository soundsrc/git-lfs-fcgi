# Defaults for git-lfs-server initscript
# sourced by /etc/init.d/git-lfs-server
# installed at /etc/default/git-lfs-server by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
